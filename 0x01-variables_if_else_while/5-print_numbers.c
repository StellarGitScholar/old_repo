#include <stdio.h>
/**
 * main - prints numbers 0-9
 *
 * Return: Always 0
 */
int main(void)
{
	char n;

	for (n = 0 ; n < 10 ; n++)
		putchar(n);

	putchar('\n');

	/* code to print numbers */

	return (0);
}
