#include <stdio.h>
/**
 * main - prints numbers 0-9
 *
 * Return: Always 0
 */
int main(void)
{
	int num;

	for (num = 0 ; num < 10 ; num++)
		putchar(num);

	putchar('\n');

	/* code to print numbers */

	return (0);
}
