#ifndef ABS_H
#define ABS_H

/**
 *Abs - this is a header file with a function to print absolute
 *	value of x
 *@x: value to print the absolute of
 */

#define ABS(x) ((x) < 0 ? (-(x)) : (x))

#endif /* ABS_H */
